meta {
  name: Logout
  type: http
  seq: 2
}

post {
  url: {{auth-service-url}}/api/v1/auth/logout
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status should be 200", function() {
    expect(res.status).to.be.oneOf([200, 204]);
  });
}

docs {
  # Logout
  Invalidate the current access token.
}
